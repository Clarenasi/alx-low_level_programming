#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 1 (Success)
 */
int main(void)
{
	char ptr[] = "and that piece of art is useful\" - Dora Korpar, 2015-10-19\n";

	size_t fwrite(const void ptr, size_t size, FILE * restrict ,stream);
	return (1);
}
